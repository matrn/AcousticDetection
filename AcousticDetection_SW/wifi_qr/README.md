# WiFi QR code generator
Run `bash gen.sh`, it parses `include/config.hpp` file and generates:
- QR code for WiFi AP connection
- QR code for url `"http://192.168.1.1/` 
- PDF file with both QR codes in different sizes